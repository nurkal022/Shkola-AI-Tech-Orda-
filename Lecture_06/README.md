# Лекция 6: RAG Architecture

## 🚀 Быстрый старт

```bash
pip install -r requirements.txt

# Последовательно:
python 01_data_loading.py              # Загрузка данных
python 02_text_chunking.py             # Сравнение chunking стратегий
python 03_embeddings_vectorstore.py    # Vector store (создаст демо-индекс)
python 04_rag_pipeline.py              # Полный RAG (создаст индекс всех книг)
python 05_advanced_rag.py              # Продвинутые техники
python 06_interactive_chat.py          # Интерактивный чат
```

## 📁 Структура

| Файл | Описание |
|------|----------|
| `01_data_loading.py` | Загрузка книг из `data/` |
| `02_text_chunking.py` | **Сравнение всех chunking методов** |
| `03_embeddings_vectorstore.py` | Embeddings + FAISS/Chroma |
| `04_rag_pipeline.py` | Базовый RAG с GPT-4.1-mini |
| `05_advanced_rag.py` | Multi-Query, Reranking, MMR |
| `06_interactive_chat.py` | Чат с памятью и стримингом |

## 📊 Chunking: Сравнение методов

```python
# CharacterTextSplitter - простой, по разделителю
# RecursiveCharacterTextSplitter - рекурсивный (РЕКОМЕНДУЕТСЯ)
# TokenTextSplitter - по токенам LLM
```

**Рекомендации:**
- `chunk_size=1000-1500` - баланс контекст/точность
- `chunk_overlap=200-300` - сохраняет контекст

## 🗄️ Vector Stores

| Store | Скорость | Сохранение | Использование |
|-------|----------|------------|---------------|
| FAISS | ⭐⭐⭐ | Ручное | Локальное |
| Chroma | ⭐⭐ | Авто | Локальное |
| Pinecone | ⭐⭐ | Облако | Production |

## 🔍 Методы поиска

1. **Базовый** - простой векторный поиск
2. **Multi-Query** - генерация альтернативных запросов
3. **Reranking** - переоценка релевантности через LLM  
4. **MMR** - Maximum Marginal Relevance (разнообразие)

## 🛠️ Технологии

- **LLM**: GPT-4.1-mini
- **Embeddings**: text-embedding-3-small
- **Vector Store**: FAISS
- **Framework**: LangChain
